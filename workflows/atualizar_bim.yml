name: Atualizar BIM (exemplo)

on:
  workflow_dispatch:

jobs:
  atualizar:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Rodar agente BIM (exemplo)
        run: python agents/agente_bim.py
